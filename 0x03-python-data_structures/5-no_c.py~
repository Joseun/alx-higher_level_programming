#!/usr/bin/python3
def no_c(my_string):
    n = len(my_string)
    for i in range(0, n):
        if my_string[i] != 'c' or my_string[i] != 'C':
            print("{:s}".format(my_string[i]), end="")
    print('')
            
